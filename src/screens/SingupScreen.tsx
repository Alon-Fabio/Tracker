import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SingupScreen = () => {
  return (
    <View>
      <Text style={styles.fontSize}>Singup Screen</Text>
    </View>
  );
};

export default SingupScreen;

const styles = StyleSheet.create({
  fontSize: {
    fontSize: 48,
  },
});
